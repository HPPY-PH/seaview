{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "staff",
        "guest"
      ]
    },
    "avatar_url": {
      "type": "string",
      "description": "Optional profile image URL from the member's identity provider"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}